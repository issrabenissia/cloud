{
  "name": "hostify-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && rm -rf node_modules package-lock.json && npm install && npm run build && cd .. && node move-build.js && cd backend && rm -rf node_modules package-lock.json && npm install",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "mysql2": "^3.14.1",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.7"
  }
}
